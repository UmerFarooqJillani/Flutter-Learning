import '../features/home/home_screen.dart';

class AppRoutes {  
  // static const String initialRoute = '/login';
  static final routes = {
    '/': (context) => HomePage(), 
    '/home': (context) => HomePage(), 
  };
}
